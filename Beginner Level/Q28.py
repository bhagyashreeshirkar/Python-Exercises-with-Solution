"""
Write a Python program that will accept the base and height of a triangle and compute the area.
"""

base = float(input('base: '))
height = float(input('height: '))
area_of_triangle = (base * height) / 2
print(f'area: {area_of_triangle}')
